# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Recent Commits on Repository' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Recent Commits on Repository
  pod 'Alamofire', '~> 4.6'
  pod 'SwiftyJSON'
  pod 'BrightFutures'
  pod 'Swinject'
  pod 'SwinjectStoryboard'
  pod 'AlamofireImage'

  target 'Recent Commits on RepositoryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Recent Commits on RepositoryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
